# MIDI constants
const MIN_MIDI_PITCH = 0
const MAX_MIDI_PITCH = 127
const MIN_MIDI_VELOCITY = 1
const MAX_MIDI_VELOCITY = 127

# Default quantization constants
const QUANTIZE_CUTOFF = 0.5
const DEFAULT_STEPS_PER_SECOND = 100

# MIDI rhythm related constants
const DEFAULT_QPM = 120.0 # Quarter notes per minute
const DEFAULT_TPQ = 220   # Ticks per quarter note
const DEFAULT_STEPS_PER_BAR = 16
const DEFAULT_STEPS_PER_QUARTER = 4